# Docker

Dockerfiles used by CI for testing.

- **`tests.Dockerfile`** — Multi-language test image (Rust, Python, Node.js, Java, .NET, Go, Ruby). Used by arm64 binding tests in CI. Built and cached by the `arm64-test-image` CI job.
