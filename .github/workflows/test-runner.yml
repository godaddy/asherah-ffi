name: Test Runner Access

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  test-github-hosted:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub-hosted runner test"
      
  test-self-hosted:
    permissions:
      contents: read
    runs-on:
      - self-hosted
      - X64
      - Linux
    steps:
      - run: echo "Self-hosted runner test"
