name: "Benchmark: Warm (MySQL warm-cache)"

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  benchmark:
    permissions:
      contents: read
    uses: ./.github/workflows/benchmark-setup.yml
    with:
      bench_mode: warm
